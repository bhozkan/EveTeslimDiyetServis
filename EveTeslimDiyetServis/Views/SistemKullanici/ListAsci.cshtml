@model IEnumerable<ETDS.Models.Asci>

@{
    ViewBag.Title = "AsciList";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<fieldset>
    <legend>Aşçılar</legend>
    <table class="table table-bordered table-striped">
        <tr>
            <th>

            </th>
            <th>
                @Html.DisplayName("Ad")
            </th>
            <th>
                @Html.DisplayName("Kullanıcı Adı")
            </th>
            <th>
                @Html.DisplayName("Cinsiyet")
            </th>
            <th>
                @Html.DisplayName("Rol")
            </th>
            <th>
                @Html.DisplayName("İl")
            </th>
            <th></th>
        </tr>
        @{int rowNo = 0;}
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @(rowNo += 1)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Kullanici.Ad) @Html.DisplayFor(modelItem => item.Kullanici.Soyad)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Kullanici.KullaniciAd)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Kullanici.Cinsiyet)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Kullanici.Rol.Ad)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Kullanici.Il.Ad)
                </td>
                <td>
                    @Html.ActionLink("Düzenle", "EditAsci", new { id = item.Id }, new { @class = "btn btn-primary" }) |
                    @Html.ActionLink("Sil", "DeleteAsci", new { id = item.Id }, new { @class = "btn btn-danger btn-md", data_toggle = "modal", data_target = "#asciSil", data_dismiss = "modal", data_backdrop = "static", data_keyboard = "false" })
                </td>
            </tr>
        }

    </table>
</fieldset>

<div class="modal fade" tabindex="-1" role="dialog" id="asciSil">
    <div class="modal-dialog">
        <div class="modal-body">
            <div class="modal-content">
                @Html.Partial("DeleteAsci", new ETDS.Models.Asci(), new ViewDataDictionary { { "Id", Model.GetEnumerator() } })
            </div>
        </div>
    </div><!-- /.modal-dialog -->
</div><!-- /.modal -->
